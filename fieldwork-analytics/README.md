# Fieldwork Analytics

Static lead-generation website for Fieldwork Analytics.

## Deploying on Netlify

This site is plain HTML/CSS/JavaScript with local assets. No build step is required.

When connecting the GitHub repo to Netlify:

- Build command: leave blank
- Publish directory: `.`

The contact form currently opens a prefilled email to `jon@fieldworkanalytics.com`.
